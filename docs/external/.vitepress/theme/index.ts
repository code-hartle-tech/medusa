// Medusa external (public) docs theme — extends VitePress default + applies
// gorgon brand tokens. Canonical: ../../../assets/brand/brand_tokens.yaml.

import DefaultTheme from 'vitepress/theme'
import './style.css'

export default DefaultTheme
