name: build + push docs image to GHCR

on:
  push:
    branches: [develop]
    paths:
      - 'docs/**'
      - '.github/workflows/docs-image.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        variant: [external, internal, landing]
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          REPO_LC="${GITHUB_REPOSITORY,,}"
          BASE="${REGISTRY}/${REPO_LC}-docs-${{ matrix.variant }}"
          echo "primary=${BASE}:sha-${{ github.run_number }}-${SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "moving=${BASE}:develop"  >> "$GITHUB_OUTPUT"

      - name: Build + push (linux/amd64)
        uses: docker/build-push-action@v6
        with:
          context: ./docs
          file: ./docs/Dockerfile
          platforms: linux/amd64
          build-args: |
            VARIANT=${{ matrix.variant }}
          push: true
          tags: |
            ${{ steps.tags.outputs.primary }}
            ${{ steps.tags.outputs.moving }}
          cache-from: type=gha,scope=${{ matrix.variant }}
          cache-to: type=gha,mode=max,scope=${{ matrix.variant }}
